import { Button } from "@mui/material";
import { BackgroundBoxes } from "../../ui/backgroundBoxes/BackgroundBoxes";

const HeroSection = () => {
  return (
    <section class="relative ">
      <BackgroundBoxes />
    </section>
  );
};

export default HeroSection;
